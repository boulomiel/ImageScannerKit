# Uncomment the next line to define a global platform for your project
 platform :ios, '18.0'

target 'ImageScannerKit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for ImageScannerKit
  pod 'TensorFlowLiteSwift', '~> 0.0.1-nightly', :subspecs => ['CoreML', 'Metal']
end

target 'ImageScannerKitTemplate' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageScannerKitTemplate

  target 'ImageScannerKitTemplateTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'ISKXcframework' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ISKXcframework

  target 'ISKXcframeworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
